version: '3.7'

services:
  indexer:
    build:
      context: .
      dockerfile: Dockerfile
    image: dexguru-indexer
    env_file:
      - .env
    environment:
      # url to clickhouse where run migrations
      - CLICKHOUSE_URL=clickhouse+http://default:@clickhouse:8123
      - OUTPUT=output.json
    restart: on-failure
    links:
      - clickhouse
      - rabbit
      - elasticsearch
    depends_on:
      - clickhouse
    command: >
      bash -c "alembic upgrade head
      && ethereumetl stream"
    volumes:
        - .:/app

  clickhouse:
    image: clickhouse/clickhouse-server:23.5.4.25
    ports:
      - "8123:8123" # HTTP interface
      - "9000:9000" # Native client interface
    volumes:
      - clickhouse_data:/var/lib/clickhouse
    ulimits:
      nofile:
        soft: 262144
        hard: 262144
    restart: unless-stopped

  rabbit:
    # Optional
    image: rabbitmq:latest
    environment:
      RABBITMQ_DEFAULT_VHOST: dex
    ports:
      - "5672:5672"
      - "15672:15672"
    restart: on-failure

  elasticsearch:
    # Optional
    image: docker.elastic.co/elasticsearch/elasticsearch:7.17.3
    environment:
      - discovery.type=single-node
      - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
      - "http.cors.enabled=true"
      - "http.cors.allow-origin=*"
    ports:
      - "9200:9200"


volumes:
  clickhouse_data:
